apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keystone-sslip
  namespace: openstack
  annotations:
    haproxy.org/path-rewrite: "/"
spec:
  ingressClassName: ingress-openstack
  rules:
  - host: horizon.10.8.132.108.sslip.io
    http:
      paths:
      - backend:
          service:
            name: horizon
            port:
              name: horz-pub
        path: /
        pathType: Prefix
